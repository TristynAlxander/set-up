# Linux
# 

alias hl="source $PREF_BIN/.hardlink.sh"
alias tex2svg="source $PREF_BIN/.tex2svg.sh"
#to-do alias tex2pdf="source $PREF_BIN/.tex2svg.sh" # Double run for bibliographies and images, also run Q quiet, also delete all the garbage, also create and remove link to bibliographies
alias sl="ls -la"
alias ls="ls -la"
alias LS="ls -la"

# Path Alias Modifications
alias ccp4_env='export PATH=$OPT/ccp4/ccp4-7.1/bin:$PATH; source ccp4.setup-sh'
alias phenix_env='source $OPT/phenix-1.19.2-4158/phenix_env.sh'
alias xds_env='export PATH=$OPT/xds:$PATH; export KMP_STACKSIZE=8m'

# Make Check
export ALIAS_CHECK=1
