# Linux

alias hl="source $PREF_BIN/.hardlink.sh"
alias commit="source $PREF_BIN/.commit.sh"
alias tex2svg="source $PREF_BIN/.tex2svg.sh"
alias sl="ls -la"
alias ls="ls -la"
alias LS="ls -la"

# Path Alias Modifications
alias ccp4_env='export PATH=$STORE/opt/ccp4/ccp4-7.1/bin:$PATH; source ccp4.setup-sh'
alias phenix_env='source $STORE/opt/phenix-1.19.2-4158/phenix_env.sh'
alias xds_env='export PATH=$STORE/opt/xds:$PATH; export KMP_STACKSIZE=8m'

# Make Check
export ALIAS_CHECK=1
